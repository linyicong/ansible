---
- name: Create user
  hosts: webservers
  remote_user: root
  vars:
    UserPasswd: "f447b20a7fcbf53a5d5be013ea0b15af" # 需要填写加密后的密码，这里使用了md5sum加密的密码，echo "xxx"|md5sum
  tasks:
    - name: Add some user
      user:
        name: {{ items }}
        password: {{ UserPasswd }}
        state: present
        shell: "/bin/bash"
        groups: video
        create_home: no
      with_items:
        - testA
        - testB
        - testC
        - zytest
