---
- name: Create user
  hosts: webservers
  remote_user: root
  vars:
    - UserPasswd: "f447b20a7fcbf53a5d5be013ea0b15af"
    - UserName: "zytest"
  tasks:
    - name: Add some user
      user:
        name: "{{ items }}"
        password: "{{ UserPasswd }}"
        state: present
        shell: "/bin/bash"
        groups: video
        create_home: no
      with_items:
        - testA
        - testB
        - testC