---
- hosts: all
  remote_user: root
  gather_facts: False
  tasks:
#  - name: include vars 
#    include_vars: test_var.yml
#
#  - name: echo customize fact info
#    debug:
#      msg: "{{ roles_list[ansible_default_ipv4.address] }}"
#
#  - name: test stat module
#    stat: path=/usr/bin/redis-server
#    register: test_stat_ret
#
#  - name: print stat info
#    debug:
#      msg: "{{ test_stat_ret }}"

  - name: test | test ansible args info
    command: which php
    changed_when: false
    failed_when: false
    register: php_installed


  - name: test | print register variable info
    debug:
      msg: "{{ php_installed }}"
